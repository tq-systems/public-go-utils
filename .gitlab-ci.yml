---
include:
- project: tq-em/common/ci
  file: template-ci.yml
  ref: master

Lint:
  extends: .go-lint

Code Generation:
  script:
    - go generate -v ./...
    - git add :/
    - git diff --exit-code HEAD
  tags:
    - tq-em-linux

Gitleaks:
  extends: .gitleaks
